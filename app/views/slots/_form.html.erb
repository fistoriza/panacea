<%= form_with(model: slot, local: true) do |form| %>
  <% if slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(slot.errors.count, "error") %> prohibited this slot from being saved:</h2>

      <ul>
      <% slot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :starting_time %>
    <%= form.datetime_select :starting_time, id: :slot_starting_time %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :slot_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
